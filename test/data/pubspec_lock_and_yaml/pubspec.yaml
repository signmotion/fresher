name: pubspec_lock_and_yaml
description: >-
  Some notes about the project.
repository: https://github.com/signmotion
topics:
  - some
  - topic

# Compatible with https://semver.org
# Format:
#   version: major.minor.patch
# You can calculate the number version code by this formula:
#   code = m[A]jor * 100 * 100 + m[I]nor * 100 + [P]atch
# Otherwise: +AAIIPP
version: 4.5.6

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ">=1.10.0 <3.0.0"
  uuid: ^4.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.2
